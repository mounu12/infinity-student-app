import { environment } from '../../environments/environment';

export class ApiUrlConstant {
    static readonly apiUrl = environment.api;
    static readonly examUrl = environment.api1;
    static readonly subjectUrl = environment.api2;
}
